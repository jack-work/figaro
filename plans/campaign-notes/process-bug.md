fig fork -S mantra="document process work" -- forked to clarify and perhaps document an issue. is this just an issue with the process registry dying out from under you?  i think you could work around it by running a transient systemd service to avoid killing it.  but if the process registry is an issue then we should document a fix for that. Im thinking of using a separate role-like form for process metadata.  we would want to add notification support for studied forms where changes to that form notify figaros that are studying it.  That would require replacing the ref counter with a registry of studying figaros.  there would be a double ref there we would need to get right, though that is already somewhat the case.  there would be a special figaro argument to the process tool that configures its background settings.  whether it notifies figaro when state changes.  the full form would get copied to the libretto so it would show up in the figaros transcript no matter what, so only a notification would be necessary when the state updates. We can tear out all the existing process registry stuff then  not something to cover now, but that would let us manage background processes such that the procmon tool works.
